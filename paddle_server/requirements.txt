# INVOEX PaddleOCR Cloud Run Service — Python Dependencies
# =========================================================
# PaddlePaddle CPU-only (Cloud Run free tier has no GPU)
paddlepaddle==3.0.0; sys_platform != "win32"
paddlepaddle==3.0.0; sys_platform == "win32"

# PaddleX (required by PaddleOCR v5 — it wraps PaddleX internally)
paddlex>=3.0.0

# FastAPI + server
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0   # headless: no GUI libs needed in containers

# PaddleOCR v5 deps (from PaddleOCR-main/requirements.txt)
shapely>=2.0
scikit-image>=0.21
pyclipper>=1.3
lmdb>=1.4
tqdm>=4.65
rapidfuzz>=3.0
PyYAML>=6.0
requests>=2.31
packaging>=23.0
