rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Secure workspaces path by enforcing user isolation
    match /workspaces/{workspaceId}/users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
  }
}
